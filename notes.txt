APIs and JWT will be used in case we are using a frontend framework as they communicate only with APIs.

to open redis gui,we open another terminal and open the parent folder and run "./node_modules/kue/bin/kue-dashboard"